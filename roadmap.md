# Marrakech (Mars Theme) boardgame.io 実装ロードマップ

この文書は、既存の HTML 実装を捨てて `boardgame.io` ベースでマルチプレイ対応のゲームとして再構築するための実装ロードマップである。
主用途は **Codex に参照させながら段階的に実装を進めること**。

---

## 0. ゴール

### 目的

* 既存の Marrakech 風ボードゲームを、`boardgame.io` を用いた構成で再実装する。
* 友人同士が各デバイスからアクセスし、同一ルームで対戦できるようにする。
* まずは **ローカル起動 / 小規模プレイ / 最小機能完成** を優先し、その後に UI 改善やデプロイを行う。

### 最終到達イメージ

* ブラウザからアクセスできる
* 3 人対戦が可能
* ルームを作成して参加できる
* 1 ターンの流れがゲーム仕様どおりに動作する
* 盤面、所持金、アッサム位置、向き、フェーズ、ログが同期される

---

## 1. 採用技術方針

### 基本構成

* ゲームエンジン: `boardgame.io`
* フロントエンド: `React`
* 言語: `TypeScript` を推奨
* 開発環境: `Vite`
* スタイリング: 最初は最低限の CSS
* 配置先: 後で検討（最初はローカル）

### 方針

* **ゲームロジックと UI を明確に分離**する
* 状態の正は `boardgame.io` 側に持たせる
* UI は `G` / `ctx` を描画し、moves を呼ぶだけに寄せる
* まずは **1 画面で完成する最小構成**を目指し、後から演出や見た目を改善する

---

## 2. 最初に固定する仕様

実装着手前に、曖昧さをなくすために以下を明文化する。

### 2.1 プレイヤー

* プレイヤー数は 3 人固定で開始する
* プレイヤーIDは `0 / 1 / 2` とし、表示上は `A / B / C`

### 2.2 盤面

* 六角形マップ（7 段）
* 行ごとのマス数: `4-5-6-7-6-5-4`
* 座標系を固定する

  * 例: `(row, col)`
  * row は `0..6`
  * col は各行の有効範囲のみ存在
* 隣接判定の仕様を座標系込みで固定する

### 2.3 地形

* 地形タイプ: `sea | mountain | city`
* 各マスは以下のいずれか

  * 空
  * 地形あり（type, owner を持つ）
* 上書き配置を許可する

### 2.4 アッサム

* 初期位置: `(3,3)`
* 初期向き: `北東`
* 向きは 6 方向とする
* 盤外に出る場合の「盤端を沿う折り返し」の仕様を厳密化する

### 2.5 ターンの流れ

1. 向き変更
2. 移動
3. 支払い判定
4. 配置（2 マス）
5. 手番終了

### 2.6 移動

* 毎ターン 1〜3 マスをランダム移動
* ランダム値はゲームログに記録する
* リプレイ性やデバッグのため、乱数生成は game ロジック内で一元管理する

### 2.7 支払い

* 着地先が他プレイヤー所有の地形なら支払い発生
* 支払い額 = 着地地形と **同タイプかつ同オーナーで連結している枚数**
* 支払先 = 地形オーナー
* 資金不足時の扱いを先に決める

  * 借金を許すか
  * 0 までしか払えないか
  * 脱落があるか
* 初版では「所持金が負になってもよい」または「持っている分だけ払う」のどちらかに固定する

### 2.8 配置

* 1 手番で同じ地形タイプを 2 マス置く
* 1 マス目:

  * アッサム隣接マスから選択
* 2 マス目:

  * 1 マス目に隣接
  * アッサム位置ではない
* 既存地形の上書きを許可
* 1 手番中に配置する 2 マスの地形タイプは同一

---

## 3. 実装全体ロードマップ

### 3.0 進捗同期（2026-04-19 時点）

実装との差分が出ないよう、現状ステータスを先に固定する。

| Phase | 状態 | メモ |
| --- | --- | --- |
| 1 | ✅ 完了 | Vite + React + boardgame.io + ローカル server 起動済み |
| 2 | ✅ 完了 | 型定義・`setup()` 実装済み |
| 3 | ✅ 完了 | 六角座標ユーティリティ・ユニットテスト実装済み |
| 4 | ❌ 未着手 | `MarrakechGame` は `ping` move のみ |
| 5 | ❌ 未着手 | 向き変更 move 未実装 |
| 6 | ❌ 未着手 | 折り返し仕様も未確定 |
| 7 | ❌ 未着手 | 支払い判定未実装 |
| 8 | ❌ 未着手 | 2 マス配置未実装 |
| 9 | 🟡 先行実装あり | HUD / 盤面表示 / ログ表示はあるが合法手 UI は未実装 |
| 10 | 🟡 先行実装あり | SocketIO 接続と MatchID / PlayerID 指定は実装済み |
| 11 | ❌ 未着手 | 例外系・終了判定未実装 |
| 12 | 🟡 一部完了 | Phase 2–3 対象テストのみ実装済み |
| 13 | ❌ 未着手 | デプロイ未着手 |

---

## Phase 1. プロジェクト雛形作成

### 目的

boardgame.io + React の最小起動環境を作る。

### タスク

* Vite + React + TypeScript プロジェクト作成
* `boardgame.io` の導入
* クライアント起動確認
* ローカルで 2〜3 タブ接続できることを確認

### 完了条件

* 開発サーバーが起動する
* 仮のゲーム状態が画面に表示される
* moves を1つ呼べる

### Codex 指示例

* React + TypeScript + Vite の雛形を作成
* boardgame.io を導入
* 最小サンプルゲームを作成し、ローカルで複数クライアント接続できる状態にする

---

## Phase 2. ドメインモデルの定義

### 目的

ゲームロジック実装前に、データ構造を固める。

### タスク

* 型定義を作る

  * `PlayerId`
  * `TerrainType`
  * `Direction`
  * `HexCoord`
  * `Tile`
  * `AssamState`
  * `GamePhase`
  * `GameLogEntry`
  * `MarrakechState`
* 初期状態生成関数を作る
* 初期配置を反映する

### 完了条件

* `setup()` で仕様どおりの初期状態が作られる
* State を console / UI 上で確認できる

### Codex 指示例

* Marrakech 用の型定義を TypeScript で作成
* 初期盤面、初期所持金、初期アッサム状態、初期地形配置を setup に実装

---

## Phase 3. 盤面座標と隣接判定の実装

### 目的

六角形盤面の基本操作を先に完成させる。

### タスク

* 有効マス判定 `isValidCell(coord)`
* 全マス列挙 `getAllCells()`
* 隣接マス列挙 `getNeighbors(coord)`
* 向きに応じた次マス計算
* テストケース作成

### 重要ポイント

* このフェーズの品質が低いと以後すべて壊れる
* 先にユーティリティとして切り出す
* 盤面ロジックは UI から独立させる

### 完了条件

* 任意マスの隣接マスが正しく取れる
* 六角形盤面で盤外判定が正しい

### Codex 指示例

* 7 段六角形マップ専用の座標ユーティリティを実装
* 有効マス判定と隣接判定に対してテストを書く

---

## Phase 4. フェーズ管理と手番進行

### 目的

1 ターンの流れを boardgame.io の moves / phases / turn で表現する。

### タスク

* フェーズ定義

  * `chooseDirection`
  * `moveAssam`
  * `placeFirstTile`
  * `placeSecondTile`
* 各フェーズで使える move を制限
* 手番終了時に次プレイヤーへ回す
* HUD 用の派生情報を計算する

### 完了条件

* 正しい順番でしか操作できない
* A → B → C の順に手番が回る

### Codex 指示例

* boardgame.io の phase 機能を使って 1 ターンの進行を実装
* 各 phase で許可される move を制限し、不正操作を弾く

---

## Phase 5. 向き変更の実装

### 目的

アッサム隣接マス選択による向き変更を実装する。

### タスク

* 隣接マスクリックから方向を決定する move を作る
* 選択可能マスのハイライト情報を返す
* UI 側で選択候補を表示する

### 完了条件

* 隣接マスを選ぶと向きが変わる
* その後 move フェーズに遷移する

### Codex 指示例

* アッサム隣接マスを選択して direction を更新する move を実装
* 隣接以外のマス指定は拒否する

---

## Phase 6. 移動と折り返し処理の実装

### 目的

Marrakech の核となるアッサム移動を完成させる。

### タスク

* 1〜3 のランダム歩数決定
* 1 歩ずつ進めるロジック実装
* 盤外時の折り返し処理実装
* 最終位置と最終向きの更新
* ログ出力

### 注意

* ここは仕様の解釈差分が出やすい
* 先に examples を複数作る
* 「この位置・向きから何歩でどこへ行くか」のテストを書く

### 完了条件

* 盤中央・辺・角付近で期待どおり移動する
* ログに歩数と最終位置が記録される

### Codex 指示例

* 六角盤上のアッサム移動ロジックを実装
* 盤外時は盤端を沿うように折り返す仕様を関数化
* 代表ケースのユニットテストを追加

---

## Phase 7. 支払い判定の実装

### 目的

着地時の支払い処理を実装する。

### タスク

* 着地マス取得
* 着地マスが他人所有か判定
* 同タイプ・同オーナー連結成分サイズを BFS / DFS で計算
* 所持金の増減処理
* ログ出力

### 完了条件

* 支払いが必要なときのみ発生する
* 連結サイズに応じて金額が正しく変わる
* 所持金が更新される

### Codex 指示例

* 着地マスから同タイプ同オーナーの連結成分サイズを求める関数を実装
* 支払い処理を move フローに組み込み、ログに記録する

---

## Phase 8. 配置フェーズの実装

### 目的

1 手番で同タイプ 2 マス配置する流れを実装する。

### タスク

* 現在選択中の地形タイプを state に保持
* 1 マス目配置 move
* 2 マス目配置 move
* 妥当性チェック

  * 1 マス目はアッサム隣接
  * 2 マス目は 1 マス目隣接
  * 2 マス目はアッサム位置不可
* 上書き配置処理
* 手番終了処理

### 完了条件

* 仕様どおり 2 マス配置できる
* 不正配置は拒否される
* 手番終了後に次プレイヤーへ移る

### Codex 指示例

* 配置フェーズを 2 段階 move に分けて実装
* 1 マス目と 2 マス目で制約条件を分離して検証する

---

## Phase 9. ログ / HUD / 最低限UI

### 目的

Phase 4〜8 のコアロジックを操作できる UI へ拡張する。

### タスク

* 既存の盤面描画を「合法手表示つき」に拡張
* フェーズごとの操作導線を追加（向き変更 / 移動 / 1マス目 / 2マス目）
* HUD 表示をロジック進行に合わせて拡張

  * 現在手番
  * フェーズ
  * アッサム位置
  * 向き
  * 所持金
  * 選択地形
* イベントログ表示を move 実装に合わせて詳細化
* 選択可能マスのハイライト

### UI 方針

* 見た目より操作性優先
* 六角形描画は CSS / SVG のどちらでもよい
* 初版は「正しく遊べること」を優先

### 完了条件

* 3 人分の情報が見える
* クリック操作で 1 ターンを最後まで進められる
* ログで何が起きたか追える

### Codex 指示例

* React で盤面コンポーネントと HUD を作成
* game state を元に合法手をハイライト表示する

---

## Phase 10. ルーム / 複数端末接続

### 目的

現状の multiplayer 接続を「実運用に耐える状態」へ仕上げる。

### タスク

* 接続失敗時 UI / リトライ導線を追加
* playerID 重複参加時の扱いを明確化
* ルーム作成 / 参加フローのバリデーション追加
* 別タブ / 別ブラウザ / 別端末での再接続確認

### 完了条件

* 複数クライアントで同じ盤面が同期される
* 各プレイヤーが自分の手番だけ操作できる

### Codex 指示例

* boardgame.io の multiplayer 構成を有効化
* ローカルネットワーク内で複数クライアント接続可能にする

---

## Phase 11. ルール仕上げと例外処理

### 目的

実プレイで壊れにくい状態にする。

### タスク

* 不正 move の全洗い出し
* 資金不足時の扱いを確定
* 勝敗条件または終了条件を定義
* 手番中断時の状態不整合チェック
* ログ文言の改善

### 完了条件

* 典型的な不正操作で壊れない
* 1 ゲーム最後まで進行できる

### Codex 指示例

* 例外ケースを洗い出してガードを追加
* 勝敗条件またはゲーム終了条件を実装する

---

## Phase 12. テスト整備

### 目的

各 Phase で追加してきたテストを統合し、回帰を防ぐ。

### 優先テスト対象

* フェーズ進行（Phase 4）
* 盤面座標
* 隣接判定
* 折り返し移動
* 連結成分サイズ
* 配置制約
* multiplayer 同期の最低限確認

### 完了条件

* コアロジックにユニットテストがある
* 修正時に主要ロジックが壊れていないことを確認できる

### Codex 指示例

* Vitest 等でゲームロジックのユニットテストを追加
* 特に移動と連結判定は高優先でカバーする

---

## Phase 13. デプロイ

### 目的

友人が各自の端末で遊べる状態にする。

### タスク

* デプロイ先を選定
* クライアントとサーバーの起動方法を整理
* 環境変数や URL 設定
* 公開 URL で接続確認

### 候補

* 小さく始めるなら、まずはローカル公開でもよい
* 常時公開するなら無料枠のあるホスティングを検討

### 完了条件

* URL を共有して友人がアクセスできる
* ルーム参加してプレイできる

---

## 4. 推奨ディレクトリ構成

```text
marrakech-boardgameio/
  src/
    game/
      MarrakechGame.ts
      setup.ts
      moves.ts
      phases.ts
      types.ts
      board.ts
      movement.ts
      payment.ts
      placement.ts
      log.ts
    components/
      Board.tsx
      HexCell.tsx
      HUD.tsx
      LogPanel.tsx
      TerrainSelector.tsx
    pages/
      GamePage.tsx
      LobbyPage.tsx
    styles/
      app.css
    main.tsx
    app.tsx
  tests/
    board.test.ts
    movement.test.ts
    payment.test.ts
    placement.test.ts
  package.json
  tsconfig.json
  vite.config.ts
```

---

## 5. 実装順の優先度

### 最優先

1. 盤面座標
2. 隣接判定
3. フェーズ進行
4. 移動
5. 支払い
6. 配置

### 後回しでよいもの

* 見た目の作り込み
* アニメーション
* ログの豪華化
* ロビーの見栄え
* スマホ向け最適化

---

## 6. 先に決めるべき未確定事項

実装前に確定しておくと手戻りが減る。  
以下は **確定済み** と **未確定** を分けて管理する。

### 6.1 確定済み（2026-04-19 合意）

1. 折り返し移動の厳密ルール
   * 1 歩ごとに盤外判定する
   * 盤外に向かう移動が発生した場合、その場で「盤外にならない向き」の中からランダムに向きを変更する
2. 移動乱数の扱い
   * 乱数生成は game ロジック内で一元管理する（クライアント側での独自乱数は使わない）
   * 歩数（1〜3）と盤外回避時の向き変更の両方で同じ乱数管理方針を使う
   * 乱数結果はログに記録する
3. 所持金不足時の扱い
   * 部分支払いを採用する（所持金が 0 を下回らない範囲で支払う）
4. ゲーム終了条件・勝敗判定
   * 終了条件は固定ターン経過
   * 勝者は所持金最大
   * 同点時は盤面上の自分所有タイル枚数が多い方を上位とする

### 6.2 未確定（要決定）

5. 配置制約の詳細
   * 同一手番 2 マスの上書き許可範囲
   * 既存タイル上書き時のログ粒度
6. 地形在庫のルール
   * 手番開始時にタイプ選択か、配置時選択か
   * 在庫 0 タイプの扱い

---

## 7. Codex 運用ルール

Codex に実装させるときは、毎回以下を明示する。

### 指示テンプレート

* 今回の対象フェーズ
* 変更対象ファイル
* 完了条件
* 既存仕様から外れてはいけない点
* テスト要否

### 例

```text
Phase 3 を実装してください。
対象は六角形盤面の座標ユーティリティです。

要件:
- 7 段、4-5-6-7-6-5-4 の六角盤
- 座標は (row, col)
- isValidCell, getNeighbors, getAllCells を実装
- TypeScript で実装
- ユニットテストを追加

完了条件:
- 全有効マス数が正しい
- 中央と端の隣接判定テストが通る
```

---

## 8. 最初の実装着手順

最初の 3 ステップは以下を推奨する。

### Step 1

プロジェクト雛形を作る

### Step 2

型定義と setup を作る

### Step 3

盤面ユーティリティとテストを書く

この 3 つができたら、その次に move / phase 実装へ進む。

---

## 9. 初版の完成ライン

初版は以下を満たせばよい。

* 3 人でターンを回せる
* 向き変更 → 移動 → 支払い → 2 マス配置 が動く
* 所持金が更新される
* ログが出る
* 盤面が同期される
* ローカルまたは限定公開で友人が遊べる

これを超える改善は、その後の第 2 段階とする。

---

## 10. 第2段階でやること

* UI 改善
* スマホ操作改善
* リプレイ
* 観戦モード
* 対戦履歴保存
* バランス調整
* AI / ソロプレイ対応

---

## 11. 一言での方針

**まずは boardgame.io 上で、盤面ロジック・ターン進行・移動・支払い・配置を正しく実装する。UI は最低限でよい。最初から完成度を求めず、ロジックをテストしながら小さく動かす。**
